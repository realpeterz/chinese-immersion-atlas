[project]
name = "chinese-immersion-atlas"
version = "0.1.0"
description = "Ingest tooling for the Chinese Immersion Atlas."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "openpyxl>=3.1",
]

[tool.uv]
package = false
